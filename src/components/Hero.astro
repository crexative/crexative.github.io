---
---

<section class="hero">
  <div class="container">
    <div class="hero-content">
      <div class="hero-text">
        <p class="subtitle">SOFTWARE AGENCY</p>
        <h1>INNOVATIVE SOFTWARE <span class="primary-color">SOLUTIONS</span></h1>
        <p class="description">
          Based in the heart of Medellín, Colombia, we are a team of expert developers dedicated to building high-quality, scalable, and custom software solutions. We transform your vision into reality with cutting-edge technology and a passion for excellence.
        </p>
      </div>
      <div class="hero-image">
        <div class="image-container">
          <img src="/images/hero.jpg" alt="Hero Image" />
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: 150px 0 80px;
    position: relative;
  }

  .hero-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 40px;
  }

  .hero-text {
    flex: 1;
  }

  .subtitle {
    font-size: 14px;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 15px;
    color: var(--text-gray);
    font-weight: 500;
  }

  h1 {
    font-size: 48px;
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 20px;
    color: var(--text-color);
  }

  .description {
    color: var(--text-gray);
    line-height: 1.6;
    margin-bottom: 30px;
    max-width: 600px;
    font-weight: 400;
  }

  .hero-image {
    flex: 1;
    display: flex;
    justify-content: center;
  }

  .image-container {
    position: relative;
    width: 100%;
    max-width: 400px;
    border-radius: 10px;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
  }

  .image-container img {
    width: 100%;
    height: auto;
    display: block;
    filter: brightness(0.9) contrast(1.1);
  }

  .image-container::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(45deg, rgba(0, 225, 199, 0.3), transparent);
  }

  @media (max-width: 768px) {
    .hero-content {
      flex-direction: column;
    }

    h1 {
      font-size: 36px;
    }
  }
</style>