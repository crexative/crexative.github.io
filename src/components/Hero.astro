---
---

<section class="hero" itemscope itemtype="https://schema.org/Organization">
  <div class="container">
    <div class="hero-content">
      <div class="hero-text">
        <p class="subtitle" itemprop="description">SOFTWARE AGENCY</p>
        <h1 itemprop="name">INNOVATIVE SOFTWARE <span class="primary-color">SOLUTIONS</span> IN MEDELLÍN</h1>
        <p class="description" itemprop="description">
          Based in the heart of <span itemprop="address" itemscope itemtype="https://schema.org/PostalAddress"><span itemprop="addressLocality">Medellín</span>, <span itemprop="addressCountry">Colombia</span></span>, we are a team of expert developers dedicated to building high-quality, scalable, and custom software solutions. We transform your vision into reality with cutting-edge technology and a passion for excellence.
        </p>
        <div class="cta-buttons">
          <a href="#contact" class="btn btn-primary" aria-label="Get a free consultation for your software project">
            Get Free Quote
          </a>
          <a href="#services" class="btn btn-secondary" aria-label="View our software development services">
            Our Services
          </a>
        </div>
      </div>
      <div class="hero-image">
        <div class="image-container">
          <img 
            src="/images/hero.jpg" 
            alt="CREXATIVE software development team working on innovative solutions in Medellín, Colombia"
            width="600"
            height="400"
            loading="eager"
            itemprop="image"
          />
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: 150px 0 80px;
    position: relative;
  }

  .hero-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 40px;
  }

  .hero-text {
    flex: 1;
  }

  .subtitle {
    font-size: 14px;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 15px;
    color: var(--text-gray);
    font-weight: 500;
  }

  h1 {
    font-size: 48px;
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 20px;
    color: var(--text-color);
  }

  .description {
    font-size: 16px;
    line-height: 1.6;
    color: var(--text-gray);
    margin-bottom: 30px;
  }

  .cta-buttons {
    display: flex;
    gap: 20px;
    margin-top: 30px;
  }

  .btn {
    padding: 15px 30px;
    border-radius: 8px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
    font-size: 16px;
    border: 2px solid transparent;
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }

  .btn-primary {
    background-color: var(--primary-color);
    color: #000;
    border-color: var(--primary-color);
  }

  .btn-primary:hover {
    background-color: transparent;
    color: var(--primary-color);
    border-color: var(--primary-color);
    transform: translateY(-2px);
    box-shadow: 0 10px 20px rgba(0, 225, 199, 0.3);
  }

  .btn-secondary {
    background-color: transparent;
    color: var(--text-color);
    border-color: var(--text-color);
  }

  .btn-secondary:hover {
    background-color: var(--text-color);
    color: var(--bg-color);
    transform: translateY(-2px);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
  }

  .hero-image {
    flex: 1;
    display: flex;
    justify-content: center;
  }

  .image-container {
    position: relative;
    width: 100%;
    max-width: 400px;
    border-radius: 10px;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
  }

  .image-container img {
    width: 100%;
    height: auto;
    display: block;
    filter: brightness(0.9) contrast(1.1);
  }

  .image-container::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(45deg, rgba(0, 225, 199, 0.3), transparent);
  }

  @media (max-width: 768px) {
    .hero-content {
      flex-direction: column;
    }

    h1 {
      font-size: 36px;
    }

    .cta-buttons {
      flex-direction: column;
      align-items: center;
    }

    .btn {
      width: 100%;
      max-width: 280px;
    }
  }
</style>